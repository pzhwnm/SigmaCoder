构建一个生产级、高效且好用的 Coding Agent，不仅取决于你使用了多么强大的大语言模型，更取决于围绕模型搭建的**基础设施（Harness 工程）**有多么扎实。软件工程几十年来积累的基础设施（测试套件、静态检查、版本控制等）天然构成了一套极其强大的 Harness，这也是 Coding Agent 成为目前所有 Agent 类型中成熟度最高、最易落地的关键原因。

根据书中的 Agent 核心框架公式： \[\textbf{Agent} = \text{LLM（Model）} + [\text{上下文} + \text{工具} + \text{约束} + \text{验证} + \text{纠正}]（\textbf{Harness}）\]

要打造一款优秀的 Coding Agent，你应该在架构设计上优先考虑以下五个核心维度的实现：

------

### 一、 上下文管理：满足 Agent 的“最低信息需求”

一个高智商的“天才新员工”在加入团队后，如果处于信息真空状态也无法写出正确的代码。在上下文工程设计中，你必须优先打通以下信息供给：

1. 代码、流程与环境的三位一体

   ：

   - **代码上下文**：通过工具精准检索代码库结构、核心数据结构及开发规范。
   - **流程约束**：在上下文或项目文件中明确 Git 分支策略、Commit 规范、CI/CD 流程。
   - **环境状态**：感知当前的操作系统类型、Shell 环境、工作目录（CWD）等。

2. 锁死项目指令文件（CLAUDE.md / .cursorrules）

   ：

   - 在代码库根目录下维护如 `CLAUDE.md` 或 `.cursorrules` 文件。这些文件包含了具体的构建与测试命令、代码风格及禁区（例如“不要改动 migrations/ 目录”）。
   - **缓存优势**：由于项目指令文件在会话中是静态不变的，将其放置于前缀位置天然对 **Prompt Cache** 极其友好，不仅能大幅降低 API 计费，还能显著缩短首字延迟（TTFT）。

3. 高密度 Agent 状态栏（Status Bar）

   ：

   - 在每次 API 调用的上下文末尾（作为 `user` 角色的消息追加），动态注入一个结构化的状态栏。
   - **注入要素**：当前工作目录（如执行 `cd` 后的实际路径）、Git 状态（分支、未暂存变更概览）、已流逝的物理时间、TODO 进度、以及工具调用计数。

4. 长输出的“头尾截断与持久化”

   ：

   - 运行测试或编译时，终端经常吐出几千行的日志。直接塞入上下文会迅速撑爆窗口并导致“上下文腐化（Context Rot）”。
   - **方案**：工具层需自动进行截断，**只保留头部前 50 行（初始输出）和尾部后 50 行（错误总结）**，中间部分用一行提示替代，并将完整大日志写入临时文件，指引 Agent 在需要时再用 `read_file` 查看。

------

### 二、 核心工具集：精简高效的“ACI 接口设计”

根据 **ACI（智能体-计算机接口）** 设计原则，工具设计应遵循“防呆（Poka-yoke）”思想，命名要直观，边界要明确，避免模糊接口被模型放大为系统性错误。你的运行时（Runtime）需要优先配齐以下七个极简工具箱：

1. **代码解释器（Code Interpreter）**：提供隔离安全的 Python 沙盒环境，用于执行符号计算或数据分析。

2. **Bash Shell**：维持一个**持久化的终端会话（Persistent Shell）**。Agent 刚用 `cd` 切换完目录，下一条命令不能又回到根目录，持久化会话能完美跨轮次保留环境变量、虚拟环境和 CWD。

3. **读/写文件工具**：支持按行号范围读取大文件的特定片段，并在返回中附带行号，防止歧义。

4. 高保真的文件编辑工具（Edit File）

   ：这是最拉开工程差距的地方。主流有两大路线：

   - **Old String \(\rightarrow\) New String（推荐起点）**：模型提供要替换的旧文本和新文本，框架执行查找替换。最稳妥、透明、无需额外模型，但大段删除时输出量大。
   - **Diff + Apply Model 路线（Cursor 方案）**：主模型专注代码逻辑，输出 diff 描述，再用一个更小、更快的 fast-apply 小模型在后台热合并。吞吐量极高，但需要额外的合并模型训练与推理投入。

5. 搜索定位组合工具（Glob & Grep）

   ：

   - 提供 **Glob**（按路径模式搜索文件名）来进行项目结构初探。
   - 提供 **Grep**（如基于 ripgrep 的正则内容匹配）来精确定位函数、变量在代码库中的出现位置。

------

### 三、 “执行-验证-反馈”的自动闭环（Verify & Correct）

可靠的 Agent 不能只靠 LLM 自我宣布“写完了”，必须由客观的验证器（Verifier）来判定是否可以停下，这也是 Loop 工程的核心课题。

1. 即时的语法反馈机制（Linter 联动）

   ：

   - 只要 Agent 调用工具修改了代码，**工具层在写入完成后应立即自动在后台运行 Linter**（如 `ruff`、`eslint` 等静态语法检查器）。
   - 如果修改引入了语法错误，直接把错误信息作为工具返回值塞给 Agent。这能让 Agent 在“打错一个括号”的第一时间自愈，而不是等到跑完整测试套件时才发现，极大缩短纠错路径。

2. 测试驱动的质量保障

   ：

   - 将“测试通过”作为任务完成的标准。Agent 编写完代码后，必须为修改的功能编写或运行测试用例。如果测试失败，Agent 应在“测试-修复”的内循环中不断迭代，直至 100% 通过。

3. 提议者-审核者（Proposer-Reviewer）多阶段角色转换

   ：

   - 在复杂的工程流中，不要用一个系统提示词包揽所有活。可以采用

     工作流编排的多阶段角色转换

     ：

     - **阶段 1：需求分析师**（提问澄清，生成 `design.md` 并等待人类审批门控）。
     - **阶段 2：软件工程师**（换上读写和执行工具，专注编码实现）。
     - **阶段 3：代码审查员**（换上 linter、测试和复杂度分析工具，进行批判性自审，若发现问题则自动回退到实现阶段）。

------

### 四、 故障边界控制与安全熔断机制

真实的生产环境存在各种物理边界限制（如网络超时、参数畸形、模型原地打转等）。

1. 重复调用检测与连续失败熔断（Circuit Breaker）

   ：

   - 对 Agent 发出的每次“工具名 + 参数”计算哈希生成**“调用指纹”**。
   - 一旦检测到相同指纹连续高频出现（无进展循环），或工具层连续报错达到阈值（如连续 3 次失败），系统必须**强行“跳闸”熔断**，优雅保存当前轨迹快照，挂起进程并升级为人工干预，防止无限循环烧干 Token 预算。

2. 空闲看门狗（Watchdog Timer）监控

   ：

   - 在运行外部 `bash` 命令或流式交互时，最危险的失败是“静默卡死”。系统需要维护独立的 Watchdog 计时器，一旦超过设定时间没有任何新输出，主动杀死挂起的进程并触发异常恢复流程。

3. 断点恢复（Session Resume）

   ：

   - 将 Agent 的轨迹（Trajectory）实时序列化并以只增不删的日志（如 JSONL 格式）写入持久化文件系统。
   - 无论进程崩溃、断电还是网络中断，只需重新加载该轨迹文件、拼上静态前缀，Agent 就能从中断处 **100% 无缝恢复执行**。

------

### 五、 安全约束：在安全网内大胆试错

1. 彻底的沙盒隔离

   ：

   - 任何代码执行（Code Interpreter、Bash 执行）必须运行在完全隔离的安全空间（如 Docker 容器、VM 或轻量级 MCP 沙盒）中，将文件系统与宿主机绝对隔离。

2. 命令行安全：语义解析而非关键字黑名单

   ：

   - 黑名单无法防御 Shell 的变量展开或参数嵌套（例如利用 `find` 的 `-exec` 参数嵌套危险载荷）。
   - 生产级外壳必须进行**语义解析**，理解命令中每个标志位的消费规则，从而精准识别并拦截嵌套在看似无害命令下的危险载荷。

3. 数据层的不变量强制约束（Invariants Enforcement）

   ：

   - 在多方委托的复杂环境下，不要指望 LLM 写的检查代码是绝对安全的。
   - **终极防线**：将核心的写入和安全性校验，作为数据库层或系统底层的“硬性不变量”进行拦截。这样即便生成的代码或 Agent 被提示注入“策反”，也绝对越不过底层系统权限的雷池。

------

📊 **开发路线实操建议**： 在开始动手写代码前，我强烈建议你先在仓库的 `ch5/coding-agent` 路径下运行并调试一下书中的 **OpenClaw** 与 **Self-Harness** 实验。实际观察一次“修改代码 \(\rightarrow\) 触发 Linter \(\rightarrow\) 测试报错 \(\rightarrow\) 状态栏更新失败计数 \(\rightarrow\) 自动修复通过”的完整闭环，能帮你建立最深刻的工程手感。