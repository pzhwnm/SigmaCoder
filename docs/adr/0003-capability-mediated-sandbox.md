# 不可信动作只能经能力中介进入沙盒

仓库内容与模型输出均视为不可信，Agent 载荷只能在 `SandboxDriver` 管理的 Linux 沙盒中运行，基准实现为 rootless OCI；网络默认拒绝，结构化进程优先，原始 Shell 受风险审批，凭据由代理或短期能力令牌提供。该决定不依赖关键词黑名单或模型自律，并允许高安全部署替换为 microVM；代价是所有 Driver 都必须通过一致的隔离、网络、资源和凭据测试，且 rootless OCI 只能降低而不能消除宿主内核风险。
