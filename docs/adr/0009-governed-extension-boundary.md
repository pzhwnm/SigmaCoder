# 固定核心工具且扩展一律进程外治理

文件、搜索、编辑、进程、终端、解释器和 Artifact 是 Harness 固定的安全关键工具族；第三方扩展在独立进程或沙盒中通过版本化 JSON-RPC 与能力清单接入，MCP 仅作为兼容适配器。扩展仍必须经过核心 schema、Policy、Approval、Artifact 和审计链，不能直连内部数据库或继承 Runner 权限；该决定牺牲进程内插件的低延迟，换取故障隔离、权限可见性和协议稳定性。
